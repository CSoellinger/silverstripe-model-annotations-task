inherit: true

build:
  nodes:
    analysis:
      environment:
        php:
          pecl_extensions:
            - ast
      tests:
        override: [php-scrutinizer-run]

checks:
  php:
    code_rating: true
    duplication: true

filter:
  paths: [src/*, tests/*]
